# Dev server overrides (copy this file to .env.local to use)

# Backend port for API proxy (auto-detected from SD.Next by default)
# BACKEND_PORT=7860

# Dev server port (default: 5174)
# DEV_PORT=5174
